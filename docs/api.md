# API Overview

FastAPI control plane for:

- machine CRUD
- tag CRUD
- OPC connection test
- OPC browse on demand
- browse cache paging/filtering
- collector reload/restart commands
- machine and tag health
- current values

Authentication is enabled by default unless `API_AUTH_DISABLED=true`.
